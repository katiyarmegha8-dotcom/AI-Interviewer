# Backend API base URL.
# Leave empty (or omit) to use the Next.js proxy (rewrites in next.config.ts).
# Set to http://localhost:8000 only if you want direct backend calls (bypass proxy).
NEXT_PUBLIC_API_URL=
